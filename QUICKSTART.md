# Быстрый старт Content Maker

## 🚀 Запуск системы

### 1. Клонирование и подготовка

```bash
git clone https://github.com/ASAFGracia/content_maker.git
cd content_maker
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте необходимые ключи:
- `OPENAI_API_KEY` - для генерации описаний через ChatGPT
- `YOUTUBE_CLIENT_ID`, `YOUTUBE_CLIENT_SECRET`, `YOUTUBE_REFRESH_TOKEN` - для загрузки на YouTube

### 3. Запуск через скрипт

```bash
./start.sh
```

Или вручную:

```bash
# Генерация ключа для Airflow
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Добавьте полученный ключ в .env как AIRFLOW_FERNET_KEY

# Запуск контейнеров
docker-compose up -d

# Инициализация Airflow
docker-compose exec airflow-webserver airflow db init
docker-compose exec airflow-webserver airflow users create \
    --username admin \
    --firstname Admin \
    --lastname User \
    --role Admin \
    --email admin@contentmaker.com \
    --password admin

# Миграции Django
docker-compose exec webapp python manage.py migrate
docker-compose exec webapp python manage.py createsuperuser
docker-compose exec webapp python manage.py init_templates
```

### 4. Доступ к сервисам

После запуска все сервисы будут доступны:

- **Web App**: http://localhost:8000
- **Airflow**: http://localhost:8080 (admin/admin)
- **Superset**: http://localhost:8088 (admin/admin)

## 🌐 Доступ через интернет

Для доступа через интернет используйте ngrok:

```bash
./setup_ngrok.sh
```

Или вручную для каждого сервиса:

```bash
# Web App
ngrok http 8000

# Airflow
ngrok http 8080

# Superset
ngrok http 8088
```

## 📝 Основные функции

### CRM система
- Управление проектами
- Задачи (как в Jira)
- Планирование встреч
- Календарь

### Видео редактор
- Загрузка видео
- Обрезка
- Наложение звука
- Вывод текста на экран
- Перевод текста (EN → RU)
- Text-to-Speech
- Ручное редактирование

### Шаблоны обработки
- Базовый шаблон
- Шаблон с текстом
- Шаблон с затемнением
- Ручное редактирование
- Легко добавлять новые шаблоны

### Автоматизация
- Автоматическая обработка видео через Airflow
- Генерация описаний через ChatGPT
- Планирование загрузки на YouTube
- Автоматическая выкладка в запланированное время

## 🔧 Управление

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f [service_name]
```

### Перезапуск сервиса
```bash
docker-compose restart [service_name]
```

## 📚 Документация

- Учетные данные: `CREDENTIALS.md`
- Полная документация: `README.md`

## ⚠️ Важно

1. Измените все пароли по умолчанию в production
2. Настройте правильные API ключи в `.env`
3. Для YouTube API требуется OAuth 2.0 авторизация
4. Убедитесь, что порты 8000, 8080, 8088 свободны

