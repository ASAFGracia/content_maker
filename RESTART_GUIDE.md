# Руководство по перезапуску проекта

## 🚨 Проблемы с интерфейсами?

Если сервисы не работают, используйте скрипт полного перезапуска.

## 🔄 Скрипты перезапуска

### 1. Полный перезапуск (рекомендуется при проблемах)

```bash
./full_restart.sh
```

**Что делает:**
- ✅ Полностью удаляет все контейнеры, volumes и образы
- ✅ Пересобирает все с нуля
- ✅ Правильно инициализирует все сервисы
- ✅ Создает пользователей заново
- ✅ Проверяет доступность сервисов

**⚠️ ВНИМАНИЕ:** Удалит все данные (базы данных, загруженные файлы)!

### 2. Обычный перезапуск

```bash
./restart.sh
```

**Что делает:**
- Перезапускает контейнеры без удаления данных
- Применяет миграции
- Инициализирует шаблоны

### 3. Перезапуск с пересборкой

```bash
./restart.sh --rebuild
```

**Что делает:**
- Пересобирает образы
- Перезапускает контейнеры
- Сохраняет данные

### 4. Полная очистка и перезапуск

```bash
./restart.sh --clean --rebuild
```

**Что делает:**
- Удаляет volumes (данные!)
- Пересобирает образы
- Запускает заново

## 🔧 Исправленные проблемы

### Web App
- ✅ Исправлена ошибка "relation 'users' already exists"
- ✅ Используется `--fake-initial` для пропуска существующих таблиц
- ✅ Автоматическое создание миграций

### Airflow
- ✅ Улучшена инициализация пользователя
- ✅ Удаление и пересоздание admin пользователя
- ✅ Правильная последовательность инициализации

### Superset
- ✅ Установка psycopg2-binary для работы с PostgreSQL
- ✅ Улучшена инициализация
- ✅ Удаление и пересоздание admin пользователя

## 📋 После полного перезапуска

### Учетные данные:

**Airflow:**
- URL: http://localhost:8080
- Логин: `admin`
- Пароль: `admin`

**Superset:**
- URL: http://localhost:8088
- Логин: `admin`
- Пароль: `admin`

**Web App:**
- URL: http://localhost:8000
- Создайте суперпользователя: `docker compose exec webapp python manage.py createsuperuser`

## 🐛 Если проблемы остаются

### Проверка логов:

```bash
# Web App
docker compose logs -f webapp

# Airflow
docker compose logs -f airflow-webserver

# Superset
docker compose logs -f superset
```

### Ручное исправление:

#### Airflow - пересоздать пользователя:
```bash
docker compose exec airflow-webserver airflow users delete admin
docker compose exec airflow-webserver airflow users create \
    --username admin \
    --firstname Admin \
    --lastname User \
    --role Admin \
    --email admin@contentmaker.com \
    --password admin
```

#### Superset - пересоздать пользователя:
```bash
docker compose exec superset superset fab delete-user admin
docker compose exec superset superset fab create-admin \
    --username admin \
    --firstname Admin \
    --lastname User \
    --email admin@contentmaker.com \
    --password admin
```

#### Django - применить миграции:
```bash
docker compose exec webapp python manage.py migrate --fake-initial
docker compose exec webapp python manage.py createsuperuser
```

## ⏱️ Время ожидания

После полного перезапуска подождите 2-3 минуты для полной инициализации всех сервисов.

## ✅ Проверка работы

```bash
# Проверить статус
docker compose ps

# Проверить доступность
curl http://localhost:8000
curl http://localhost:8080/health
curl http://localhost:8088
```

