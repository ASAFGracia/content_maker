# Безопасный коммит в GitHub

## ✅ Проверка перед коммитом

### 1. Убедитесь, что .env файл не коммитится

```bash
# Проверьте, что .env игнорируется
git check-ignore .env

# Проверьте статус - .env не должен быть в списке
git status
```

### 2. Проверьте, что нет секретных данных в коде

```bash
# Поиск возможных секретов в файлах
grep -r "ContentMaker2024!Secure" --exclude-dir=.git --exclude-dir=node_modules .
grep -r "your-secret-key" --exclude-dir=.git --exclude-dir=node_modules .
```

**Важно:** В коде есть дефолтные значения для примера, но они должны быть только в `.env.example`, а не в реальных `.env` файлах.

### 3. Что НЕ должно быть в коммите:

- ❌ `.env` файл
- ❌ `.env.local` файл
- ❌ Любые файлы с паролями, ключами, токенами
- ❌ `media/` директория (загруженные файлы)
- ❌ `airflow/logs/` (логи могут содержать чувствительные данные)
- ❌ `*.db` файлы (базы данных)
- ❌ `__pycache__/` директории

### 4. Что ДОЛЖНО быть в коммите:

- ✅ `.env.example` (шаблон без реальных значений)
- ✅ `.gitignore` (правильно настроенный)
- ✅ Весь исходный код
- ✅ Docker конфигурации
- ✅ Документация

## 🔒 Безопасный процесс коммита

### Шаг 1: Проверка перед коммитом

```bash
# Проверьте, что будет закоммичено
git status

# Проверьте изменения
git diff

# Убедитесь, что .env не в списке
git status --ignored | grep .env
```

### Шаг 2: Добавление файлов

```bash
# Добавьте все файлы (кроме игнорируемых)
git add .

# Или добавляйте файлы по одному
git add docker-compose.yml
git add webapp/
# и т.д.
```

### Шаг 3: Проверка перед коммитом

```bash
# Посмотрите, что будет закоммичено
git status

# Проверьте, нет ли случайно .env
git diff --cached | grep -i "password\|secret\|key" | head -20
```

### Шаг 4: Коммит

```bash
git commit -m "Описание изменений"
```

### Шаг 5: Отправка в GitHub

```bash
# Проверьте, что вы отправляете правильную ветку
git log --oneline -5

# Отправьте в репозиторий
git push -u origin main
```

## 🚨 Если случайно закоммитили секреты

### Немедленные действия:

1. **Смените все пароли и ключи:**
   - Пароли в базе данных
   - API ключи
   - Токены доступа

2. **Удалите секреты из истории Git:**

```bash
# Удалите файл из истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all

# Или используйте BFG Repo-Cleaner (рекомендуется)
# https://rtyley.github.io/bfg-repo-cleaner/
```

3. **Принудительно обновите удаленный репозиторий:**

```bash
git push origin --force --all
```

⚠️ **Внимание:** Это перезапишет историю. Убедитесь, что никто другой не работает с репозиторием.

## 📋 Чеклист перед каждым коммитом

- [ ] `.env` файл не в списке `git status`
- [ ] Проверены все изменения на наличие секретов
- [ ] `.gitignore` правильно настроен
- [ ] Нет хардкода паролей в коде
- [ ] Все секреты в переменных окружения
- [ ] `.env.example` обновлен (без реальных значений)

## 🔐 Рекомендации по безопасности

1. **Никогда не коммитьте:**
   - Реальные пароли
   - API ключи
   - Токены доступа
   - Приватные ключи SSH
   - Сертификаты

2. **Используйте:**
   - `.env` файлы (в .gitignore)
   - Переменные окружения
   - Секреты в CI/CD системах (GitHub Secrets, GitLab CI Variables)
   - Менеджеры секретов (HashiCorp Vault, AWS Secrets Manager)

3. **В коде используйте:**
   - Дефолтные значения только для разработки
   - Четкие инструкции в README
   - `.env.example` с примерами (без реальных значений)

## ✅ Текущий статус безопасности

Проверьте сейчас:

```bash
# 1. .env игнорируется?
git check-ignore .env && echo "✅ .env безопасен"

# 2. Нет секретов в коммитах?
git log --all --full-history --source -- "*secret*" "*password*" "*key*" | head -5

# 3. .env.example есть, .env нет?
ls -la .env* && echo "✅ Только .env.example должен быть в репозитории"
```

