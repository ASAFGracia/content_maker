# 🚀 Быстрый запуск Content Maker

## Простой запуск одной командой:

```bash
./start.sh
```

Скрипт автоматически:
- ✅ Создаст .env файл если его нет
- ✅ Сгенерирует ключи для Airflow
- ✅ Создаст необходимые директории
- ✅ Запустит все Docker контейнеры
- ✅ Инициализирует базы данных

## После запуска откройте в браузере:

- **Web App**: http://localhost:8000
- **Airflow**: http://localhost:8080 (admin/admin)
- **Superset**: http://localhost:8088 (admin/admin)

## 📤 Отправка в GitHub:

```bash
git push -u origin main
```

Если репозиторий еще не создан на GitHub:
1. Создайте репозиторий на https://github.com/ASAFGracia/content_maker
2. Затем выполните `git push -u origin main`

## 🔍 Проверка работы:

```bash
# Статус контейнеров
docker compose ps

# Логи
docker compose logs -f webapp
docker compose logs -f airflow-webserver
```

## ⚠️ Если что-то не работает:

1. Проверьте логи: `docker compose logs [service_name]`
2. Пересоберите контейнеры: `docker compose up -d --build`
3. Остановите и запустите заново: `docker compose down && ./start.sh`

## 📝 Создание суперпользователя Django:

```bash
docker compose exec webapp python manage.py createsuperuser
```

После этого вы сможете войти в админ-панель Django по адресу http://localhost:8000/admin/

