# Content Maker - Система для создания и управления видеоконтентом

Комплексная платформа для ютуберов с CRM, аналитикой и автоматизацией процессов создания контента.

## 🚀 Возможности

- **CRM система** - управление проектами, задачами и встречами
- **Видео редактор** - загрузка, редактирование и обработка видео
- **Шаблоны обработки** - готовые шаблоны для быстрой обработки видео
- **Автоматизация** - автоматическая выкладка на YouTube с генерацией описаний
- **Планирование** - календарь, задачи и встречи
- **Аналитика** - интеграция с Superset для визуализации данных
- **Оркестрация** - Airflow для управления процессами

## 📋 Требования

- Docker и Docker Compose
- Python 3.10+ (для локальной разработки)

## 🛠 Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ASAFGracia/content_maker.git
cd content_maker
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Сгенерируйте ключи для Airflow:
```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```
Добавьте полученный ключ в `.env` как `AIRFLOW_FERNET_KEY`

4. Запустите все сервисы:
```bash
docker-compose up -d
```

5. Инициализируйте Airflow (первый запуск):
```bash
docker-compose exec airflow-webserver airflow db init
docker-compose exec airflow-webserver airflow users create \
    --username admin \
    --firstname Admin \
    --lastname User \
    --role Admin \
    --email admin@contentmaker.com \
    --password admin
```

## 🌐 Доступ к сервисам

После запуска все сервисы будут доступны по следующим адресам:

- **Web App (CRM + Видео редактор)**: http://localhost:8000
- **Airflow**: http://localhost:8080 (admin/admin)
- **Superset**: http://localhost:8088 (admin/admin)
- **PostgreSQL**: localhost:5432

### Доступ через интернет

Для доступа через интернет используйте ngrok или аналогичный сервис:

```bash
# Установите ngrok
brew install ngrok  # macOS
# или скачайте с https://ngrok.com/

# Запустите туннель для каждого сервиса
ngrok http 8000  # Web App
ngrok http 8080  # Airflow
ngrok http 8088  # Superset
```

## 🔐 Учетные данные по умолчанию

### PostgreSQL
- **Host**: localhost
- **Port**: 5432
- **Database**: content_maker
- **Username**: content_admin
- **Password**: ContentMaker2024!Secure (измените в `.env`)

### Airflow
- **Username**: admin
- **Password**: admin (измените после первого входа)

### Superset
- **Username**: admin
- **Password**: admin (измените после первого входа)

## 📁 Структура проекта

```
content_maker/
├── airflow/          # DAG'и и конфигурация Airflow
├── webapp/           # Django приложение (CRM + Видео редактор)
├── superset/         # Конфигурация Superset
├── media/            # Загруженные файлы
├── docker-compose.yml
└── README.md
```

## 🔧 Настройка

### OpenAI API

Для генерации описаний к видео через ChatGPT добавьте в `.env`:
```
OPENAI_API_KEY=your-api-key-here
```

### YouTube API

Для автоматической выкладки видео на YouTube:
1. Создайте проект в Google Cloud Console
2. Включите YouTube Data API v3
3. Создайте OAuth 2.0 credentials
4. Добавьте в `.env`:
```
YOUTUBE_CLIENT_ID=your-client-id
YOUTUBE_CLIENT_SECRET=your-client-secret
YOUTUBE_REFRESH_TOKEN=your-refresh-token
```

## 📝 Лицензия

MIT

